# Product mockups

Every product picture on the site is one of these server components, drawn from the real app
(InkOS, `/Users/yashbaldha/Desktop/Boldteq App/InkOS`) and filled from `sample-data.ts`.
Before/after = the same screen twice with different props.

## Rules

- **One studio.** Every screen carries `SampleTag` ("Sample studio"). No studio name, city, logo or invented brand.
- **One day.** Thursday, Oct 8, 2026, 10:40. Use `TODAY_LABEL` / `TODAY_DATE_LINE` / `NOW`. Any new date: check the weekday first (valid: Mon Oct 5, Tue Oct 6, Wed Oct 7, Thu Oct 8, Fri Oct 9, Sat Oct 10, Thu Nov 5, Sat Nov 7, Tue Nov 10, Fri Nov 13, Sat Nov 14; every date already in `sample-data.ts` was checked). Anything that has already happened must be before Thu 10:40.
- **Numbers come from `sample-data.ts`.** Totals are computed there (`DEPOSITS_HELD_TODAY_CENTS` = $340 = Asha's $240 pool + Jo's $100, `DEPOSITS_APPLIED_TODAY_CENTS` = $80 (Priya), `WEEK_GROSS_CENTS` = $7,630, `WEEK_PAID_OUT_CENTS` = $5,622, `IN_DEPOSIT_POOLS_CENTS` = $600, `COMMISSIONS_OWED_CENTS` = $1,236 …). Never type a total by hand; format with `usd()`.
- **Policies.** Cancellation: inside `CANCEL_WINDOW_HOURS` (48) the deposit is kept, as on the homepage. Deposit requests lapse after `DEPOSIT_DEADLINE_HOURS` (24, the app's `studios.deposit_deadline_hours` default).
- **The app's words.** Labels, tabs, statuses and columns below are copied from the app source listed. Say "Projects" (not "Sleeves"), "session", "consult", "deposit pool", "red ink". Messages channels: SMS and email only.
- **Nothing the app doesn't have:** no no-show risk score, healing/sterilization blocks, loyalty programme, "Studio P&L", portfolio auto-sync, Apprentice role.
- **AI never draws.** `AiMoodboardScreen` shows the client's own references (`PhotoTile` "R1"…) and a text brief the artist edits.
- **Never fake tattoo art.** Photos are `PhotoTile` (canvas-deep block + serif label "S1", "P2", "F3").
- **Decorative.** `AppFrame` and phone shells are `aria-hidden`. Inside a mockup use only `span`/`div`/`p`/table markup: no headings, links, buttons or inputs.
- **Mobile.** Phone screens stay phone-sized (`AppShellPhone`). Dense tables go in `AppTable` (scrolls inside the frame, sticky header, right-edge fade; pick `minWidth`). Card layouts stack with container queries on the frame body (`@md:`, `@xl:`, `@2xl:` …), never viewport breakpoints. The page itself must never scroll sideways.
- **Colour.** App chrome uses the `app-*` tokens only (sand sidebar, peach active pill, status tones). The one extra colour is the app's rust primary, which lives inside `AppButton variant="primary"`. Public app pages (booking, consent, portal) use `variant="onyx"`.

## Foundation (this folder)

| File | Exports |
|---|---|
| `sample-data.ts` | `STUDIO_LABEL`, `TODAY_LABEL`, `TODAY_DATE_LINE`, `NOW`, `VIEWER` ("Good morning, Dev"), `SAMPLE_PLAN` (pro), `usd()`; `ARTISTS` / `ARTIST_ORDER` (Dev ember, Mara info, Rio warning tone), `payRuleLabel`, `TEAM`, `PERMISSION_ROLES` / `PERMISSIONS`; `TODAY_SESSIONS`, `TODAY_BOOKINGS`, `DAY_START_MIN` / `DAY_END_MIN`, `BOOKING_STATUS_LABEL`; `DEPOSITS` (pending rows carry `requestedAt` / `dueAt`), `DEPOSITS_HELD_TODAY_CENTS`, `DEPOSITS_APPLIED_TODAY_CENTS`, `DEPOSITS_PENDING(_CENTS)`, `DEPOSIT_DEADLINE_HOURS`, `CANCEL_WINDOW_HOURS`, `LATE_CANCEL` (`piece`, `day`, `startMin`, `slot`, `cancelledAt`, `cents`, `policy`); `TRANSACTIONS`, `REVENUE_TODAY_CENTS`, `REVENUE_YESTERDAY_CENTS`, `COMMISSIONS`, `COMMISSIONS_OWED_CENTS`; `WEEKLY_REVENUE` (8 weeks), `weekTotalCents`, `PAYOUT_WEEK`, `WEEK_PAYOUTS`, `WEEK_GROSS/PAID_OUT/STUDIO_CENTS`, `ARTIST_STATS`, `SESSIONS_8W`, `NO_SHOW_PCT_8W`, `SESSIONS_BY_WEEKDAY`, `NEW_VS_RETURNING`, `ACQUISITION_SOURCES`; `PROJECTS`, `ASHA_PROJECT`, `PROJECT_STATUS_LABEL`, `IN_DEPOSIT_POOLS_CENTS`; `CLIENTS`, `ELENA_ALLERGY`; `NEEDS_ATTENTION`; `THREADS`, `UNREAD_THREADS`, `JO_THREAD`, `JO_SUGGESTED_REPLY`; `FORM_TEMPLATES`, `SUBMISSIONS`; `INVENTORY`, `INVENTORY_LOW`, `INVENTORY_REACH`, `INVENTORY_VALUE_CENTS`; `SEGMENTS`, `CAMPAIGNS`, `TEXTS_THIS_MONTH`, `WAITLIST`, `REFERRAL`; `FLASH_EVENTS`, `flashEventValueCents`, `FLASH`, `FLASH_AVG_PRICE_CENTS`, `PORTFOLIO`; `AI_BRIEF`; `LOCATIONS` |
| `app-sidebar.tsx` | `AppSidebar({active, lens})`, `AppNavItem` (ids: `today`, `needs-attention`, `messages`, `calendar`, `appointments`, `projects`, `waitlist`, `clients`, `team`, `payments`, `services`, `forms`, `inventory`, `flash`, `flash-events`, `portfolio`, `locations`, `analytics`, `marketing`, `ai`), `AppLens` (`studio` \| `artist` \| `desk`), `appNavLabel()` |
| `app-frame.tsx` | `AppFrame({active, lens?, title?, greeting?, meta?, actions?, sidebar?, className?, bodyClassName?, children})`. Body is an unnamed `@container`; no padding (screens add their own, `px-4 @lg:px-6`). Top bar: the title holds its width from `@sm/frame`; the meta line gives way first, then whole actions drop out from the left (the primary, last, stays). Below 19rem (phones under ~375) the menu icon goes, the title is 12px and the tag tightens. Titles up to ~19 characters ("Roles & permissions") fit at 320. |
| `app-parts.tsx` | `AppLabel`, `AppKpi`, `AppKpiStrip({items})` (each tile is a `@container/kpi`: the value scales to the tile so it never clips; under 7.5rem the label is 10px and the accent dot goes), `AppTable({columns, rows, minWidth?, maxHeight?})`, `AppTabs({tabs, active})` (active tab always whole; neighbours stay only while they fit, never cut), `AppStatus({tone})` (`success` `warning` `danger` `info` `neutral` `active`), `AppBookingStatus`, `AppProjectStatus`, `AppButton({variant})` (`primary` rust, `onyx`, `secondary`, `ghost`), `AppAvatar({initials, tone, size})`, `NowLine({time, top})`, `PhotoTile({label, caption?, aspect?})`, `AppCard({title?, meta?, padded?})` |

Sidebar rail: every item (row or group label) is a 28px slot + 1px gap and the scroller's height is rounded down to that 29px grid, so no row is ever cut at either edge. The active row snaps so its group label sits at the top (or as close as the list allows). The visible list never ends on a group label: at a height where a label would be the last whole slot, the rail stops one slot earlier (pure CSS, `railHeight` in `app-sidebar.tsx`).

Sidebar source: `components/app/sidebar/sidebar-nav.ts` (`STUDIO_NAV`, `ARTIST_NAV`, `RECEPTION_NAV`, icons in `ID_META`) and `SidebarParts.tsx` (`ViewSwitch`, `NavBadge`). The app nests Clients/Team under an expandable "Users" row and Service List/Booking under "Services"; the mockup shows them flat. The artist lens labels Payments "Earnings"; the desk lens labels Today "Front desk".

Theme source: `theme/palette.css`, `theme/semantic.css` (sidebar sand, `--palette-rust-200` active pill, rust-700 CRM primary, status tones). Screenshots in `sweep-shots/*.png` (Aug 17) show the old dark sidebar: use them for layout and content only.

## Screens → app

App paths are relative to InkOS. `(app)` = `app/(app)`.

| Screen (file · export) | App route | App source | Key labels used |
|---|---|---|---|
| `today.tsx` · `TodayScreen` | `/dashboard` (nav "Today") | `(app)/dashboard/_proto/ScrDashboard.tsx`, `DashWidgets.tsx`, `adapt.ts` | "Good morning, Dev" + "Thursday, October 8"; actions New booking · Payments · Walk-in; KPIs Revenue · Bookings ("on the deck today") · Deposits pending ("at risk") · Commissions owed ("N pending approval"); Next up; Check in; Deposit paid / Deposit pending / Deposit due; Send form |
| `needs-attention.tsx` · `NeedsAttentionScreen` | `/inbox` (nav "Needs attention") | `(app)/inbox/_proto/Inbox.tsx`, `lib/inbox/queue.ts` | Needs attention; Resolve · Snooze · Mark read; Resolved; items: unpaid deposits, unsigned forms, low stock |
| `messages.tsx` · `MessagesScreen` | `/messages` | `(app)/messages/_proto/ScrMessages.tsx`, `thread-list.tsx`, `thread-view.tsx`, `composer.tsx`, `context-pane.tsx`, `request-deposit-sheet.tsx`, `consent-send-sheet.tsx`, `quick-responses.tsx` | Messages; channel Text / Email / Text or email; Deposit (context pane); request deposit; send consent form; suggested reply the artist approves |
| `calendar.tsx` · `CalendarScreen({view})` | `/calendar` | `(app)/calendar/_proto/ScrCalendar.tsx` | views day · week · month · agenda; All stations; Book; Block time; Move booking; now-line |
| `appointments.tsx` · `AppointmentsScreen` | `/appointments` | `(app)/appointments/_proto/Appointments.tsx`, `data.ts` (`APPT_STATUS`), `lib/bookings/constants.ts` | columns When · Client · Artist · Service · Status · Deposit · Price; Pending · Confirmed · In progress · Completed · Cancelled · No-show; Session total |
| `booking-flow.tsx` · `BookingFlowPhone({step})` | `/book/[studioSlug]`, `/book/[studioSlug]/[serviceId]`; set up at `/booking-page` | `app/book/[studioSlug]/page.tsx`, `(app)/booking-page/_proto/ScrBookingPage.tsx` | steps Artist → Time → Details → Intake; deposit to hold the slot; onyx primary (public page) |
| `client-file.tsx` · `ClientFileScreen({tab})` | `/clients` (detail) | `(app)/clients/_proto/ClientDetail.tsx`, `data.ts` (`CLIENT_TABS`), `detail-tabs.tsx`, `detail/tab-overview.tsx`, `tab-sessions.tsx`, `tab-consent.tsx`, `tab-financial.tsx`, `tab-gallery.tsx`, `tab-notes.tsx`, `tab-comms.tsx`, `detail/shared.tsx` | tabs Overview · Sessions · Gallery · Notes · Comms · Consent · Financial; deposit pool Paid in · Applied · Available · Refundable; allergy flag |
| `forms.tsx` · `FormsScreen({tab})` | `/forms` | `(app)/forms/_proto/ScrForms.tsx`, `TemplatesTab.tsx`, `SubmissionsTab.tsx`, `KioskFlow.tsx`, `lib.tsx` | tabs Templates · Submissions; Kiosk mode; Send to client; Copy public link; QR — in-studio; templates Tattoo consent — general · Medical history · Aftercare acknowledgement · Touch-up waiver · Minor / guardian consent · Photo & social release |
| `forms.tsx` · `ConsentSignPhone` | `/consent/[token]`, kiosk `/consent/kiosk/[kioskToken]` | `app/consent/[token]/page.tsx`, `app/consent/kiosk/[kioskToken]/page.tsx` | client signs on their phone; signed copy stored as a PDF that can't be edited |
| `briefing.tsx` · `BriefingPhone` | `/briefing/[bookingId]` | `app/briefing/[bookingId]/page.tsx`, `components/briefing/BriefingHeader.tsx`, `PinnedNotes.tsx`, `DepositRow.tsx`, `FormsRow.tsx`, `LastSessions.tsx`, `ProjectState.tsx`, `StartSessionButton.tsx` | pinned notes (red ink allergy), deposit, forms, last sessions, project state, start session |
| `client-portal.tsx` · `ClientPortalPhone` | `/portal/[projectId]` | `app/portal/[projectId]/page.tsx`, `components/portal/FinancialSection.tsx` | Applied · Available (deposit pool); Paid in full; next session |
| `projects.tsx` · `ProjectsScreen({view})` | `/projects`, session `/projects/[id]/sessions/[sessionId]` | `(app)/projects/_proto/ScrProjects.tsx`, `ProjectsList.tsx`, `shared.tsx`, `data.ts` (`PROJ_STATUS`) | views Gallery · Board · List; Planning · Deposit Paid · Active · Healing · Complete · On Hold · Cancelled; In deposit pools; Avg completion; Paid in full |
| `portfolio.tsx` · `PortfolioScreen({library})` | `/portfolio`, `/flash` | `(app)/portfolio/_proto/ScrPortfolio.tsx`, `parts.tsx` | Pieces · Featured · Healed · Styles; flash Available · Reserved · Avg. price; Bulk upload; Preview public page |
| `flash-events.tsx` · `FlashEventsScreen` | `/flash-events` | `(app)/flash-events/_proto/FlashEventsList.tsx`, `FlashEventDetail.tsx`, `FlashEventTabs.tsx` | Flash events; Scheduled · Slots booked · Fill rate · Booked value |
| `ai-moodboard.tsx` · `AiMoodboardScreen` | `/ai-design` (nav "AI") | `(app)/ai-design/_proto/ScrAI.tsx`, `(app)/ai-design/[id]/_proto/AiRequestDetailProto.tsx` | AI Design Assistant; Placement · Size · Style · Suggested palette; Earlier directions; references are the client's uploads |
| `payments.tsx` · `PaymentsScreen({tab})` | `/payments?tab=` | `(app)/payments/_proto/ScrPayments.tsx`, `kpi.tsx` | tabs Transactions · Commissions · Payroll · Disputes; Recent transactions; Commissions owed · Pending deposits · Paid to date · Commission this run · Artist earnings this run · Studio retains; Disputes & chargebacks · Open disputes · Respond before |
| `team.tsx` · `TeamScreen({view})` | `/team` | `(app)/team/_proto/ScrTeam.tsx`, `components/settings/team/PermissionsMatrix.tsx` | columns Name · Role · Specialties · Joined · Last active; roles Owner · Admin · Artist · Front desk · Guest; Active · Pending; Permissions matrix (Owner · Admin · Artist · Receptionist · Guest Artist; View all bookings, Cancel bookings, View own bookings only, View clients, Export client data, View / Edit commission rates, Manage billing) |
| `guest-artists.tsx` · `GuestArtistsScreen` | `/guest-artists` | `(app)/guest-artists/page.tsx`, `(app)/guest-artists/[id]/page.tsx` | Rio · guest spot Fri, Oct 2 – Fri, Oct 9 · 70/30; seat ends with the spot |
| `inventory.tsx` · `InventoryScreen({tab})` | `/inventory` | `(app)/inventory/_proto/ScrInventory.tsx`, `ItemsTab.tsx`, `MovementsTab.tsx`, `POsTab.tsx`, `SuppliersTab.tsx`, `kpi.tsx` | tabs Items · Movements · Purchase orders · Suppliers; KPIs Items tracked · Low / out · REACH-registered · Stock value |
| `analytics.tsx` · `AnalyticsScreen({tab})` | `/analytics/revenue`, `/analytics/artists`, `/analytics/clients`, `/analytics/bookings` | `(app)/analytics/_proto/ScrAnalytics.tsx`, `revenue/_proto/ScrRevenueReport.tsx`, `artists/_proto/ScrArtistsReport.tsx`, `clients/_proto/ScrClientsReport.tsx`, `bookings/_proto/ScrBookingsReport.tsx` | Revenue trend; Artist leaderboard; Resident / Guest; New vs returning; Acquisition source; By day of week; By service; Daily / Weekly |
| `marketing.tsx` · `MarketingScreen({tab})` | `/marketing?tab=` | `(app)/marketing/_components/AudienceTab.tsx`, `SegmentChipsRow.tsx`, `RecentCampaignsList.tsx`, `WaitlistTab.tsx`, `WaitlistFilterPills.tsx`, `components/marketing/SegmentPresetChips.tsx`, `app/api/marketing/referrals/program/route.ts` | tabs Audience · Campaigns · Waitlist · Referral program; segments All clients · Inactive 90 days · New (last 30 days) · Top spenders · Booked next 14 days; waitlist Active · Offered · Booked · Expired · Cancelled |
| `locations.tsx` · `LocationsScreen` | `/locations` | `(app)/locations/_proto/ScrLocations.tsx`, `ScrLocationDetail.tsx` | Total locations · Total artists · Bookings (month) · Revenue (month); Add location |

## The sample studio in one screen

- **Plan:** Pro (guest seats, roles). **Team:** Dev (Owner, resident, 60% commission), Mara (Artist, resident, booth rent $250/wk), Rio (Guest, Fri Oct 2 – Fri Oct 9, 70/30), Noor (Front desk), Cam (Admin, invite pending).
- **Today, Thu Oct 8:** Dev 10:00–1:00 Asha M. koi sleeve session 4 of 5 (in the chair) · Mara 11:00–12:00 Jo K. consult, fine-line florals, $100 deposit paid · Rio 12:00–5:00 walk-in flash, guest day, 3 slots · Mara 1:00–3:00 Priya S. fine-line florals, $80 deposit applied today, consent not signed yet · Dev 1:30–4:00 Elena R. back piece session 2 of 3, red ink allergy (no red today, patch test before session 3, consent signed 9:42 on her phone) · Mara 4:30–5:00 Sam T. touch-up, forearm script, consent signed.
- **Money:** deposits held for today $340 = Asha's $240 (pool, sessions 4–5; $120 of it applies at today's checkout) + Jo's $100 (goes toward the tattoo) · Priya's $80 applied to today's session (not held) · Elena's pool $320 paid in / $160 applied to session 1 / $160 held for session 3, nothing held for today (session 2 is paid at checkout) · Asha's deposit pool $300 paid in / $60 applied / $240 available / $0 refundable, session 5 Sat Nov 7, 11:00 · week Sep 28 – Oct 4: Dev 60% of $3,420 = $2,052, Mara $2,910 − $250 = $2,660, Rio 70% of $1,300 = $910; $7,630 gross, $5,622 paid out.
- **Leo B.'s late cancel:** Mara, chest panel session 1, Fri Oct 9, 11:00 (homepage: "Chest panel · S1"). Cancelled Wed Oct 7, 6:12 PM, inside the 48-hour window, so the $150 deposit is kept. The freed slot is offered to Nadia H. from the waitlist.
- **Supporting clients** (use these, never invent more): **Owen P.** (Dev; new calf piece Sat Oct 10, 1:00, booking pending; $150 deposit requested Wed Oct 7, 7:30 PM, due today 7:30 PM under the 24-hour default, so it sits in today's lane, not overdue; he texted Wed "Will pay the deposit tonight"; earlier piece a traditional panther, Sat Aug 15; a medical-history link sent Tue Sep 29 lapsed unsigned) · **Kira N.** (Mara; new client; fine-line wrist Sat Nov 14, 12:00; $150 deposit paid today 8:05 AM; on the waitlist for an earlier slot) · **Bea L.** (Dev; botanical half sleeve; session 2 Wed Oct 7 = $600: $500 paid + $100 from her pool; session 3 Tue Nov 10, 2:00; $100 deposit paid today 7:12 AM) · **Tomás V.** (Dev; chest panel; session 1 Tue Oct 6, $900; session 2 on the waitlist) · **Nadia H.** (Mara's waitlist, fine-line, small, Fri or Sat; offered Leo B.'s slot).
